# RAG 金融问答模块 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 nanocode 添加 RAG 金融问答功能，用户可通过对话从本地知识库检索金融研报/新闻中的信息，获得带引用来源的回答。

**Architecture:** LlamaIndex 负责文档加载、分块、向量索引构建（Chroma 持久化），检索结果由 Python 层拼装为 context prompt，通过现有 `call_api()` 发送给 LLM 合成回答。RAG 作为 `rag_query` 和 `rag_ingest` 两个工具注册到 agentic loop。

**Tech Stack:** Python 3.10+, LlamaIndex (core + readers-file + embeddings-openai + vector-stores-chroma), ChromaDB, PyMuPDF, python-dotenv

**Design Spec:** `docs/superpowers/specs/2026-04-29-rag-qa-design.md`

---

## File Structure

| File | Responsibility | Status |
|------|---------------|--------|
| `pyproject.toml` | uv 项目配置和依赖声明 | Create |
| `.env` | 添加 Embedding API 配置 | Modify |
| `.gitignore` | 添加 data/chroma_db 和 .ingested.json | Modify |
| `config.py` | 所有配置集中管理 | Create |
| `tools.py` | 8 个工具实现（原 6 + rag_query + rag_ingest）+ make_schema | Create |
| `rag/__init__.py` | 暴露 `ingest()` 和 `query()` 公共接口 | Create |
| `rag/loader.py` | 文档加载（PDF/Markdown/纯文本） | Create |
| `rag/chunker.py` | 文本分块（SentenceSplitter） | Create |
| `rag/indexer.py` | 索引构建与增量管理（Chroma + LlamaIndex） | Create |
| `rag/retriever.py` | 相似度检索（top-k） | Create |
| `prompts/rag_system.txt` | RAG 场景系统提示词 | Create |
| `data/documents/` | 原始文档目录 | Create |
| `nanocode.py` | 主程序（重构为导入模块） | Modify |

---

### Task 1: Project Setup

**Files:**
- Create: `pyproject.toml`
- Modify: `.env`
- Modify: `.gitignore`

- [ ] **Step 1: Create pyproject.toml**

```toml
[project]
name = "nanocode-financial"
version = "0.1.0"
description = "个人金融智能分析助手"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "llama-index-core",
    "llama-index-readers-file",
    "llama-index-embeddings-openai",
    "llama-index-vector-stores-chroma",
    "chromadb",
    "pymupdf",
]
```

- [ ] **Step 2: Add embedding config to .env**

在 `.env` 末尾追加以下内容（不要修改已有的 API 配置）：

```
# Embedding API Configuration
EMBEDDING_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v3

# RAG Paths
CHROMA_PERSIST_DIR=data/chroma_db
DOCUMENTS_DIR=data/documents
```

- [ ] **Step 3: Update .gitignore**

在 `.gitignore` 末尾追加：

```
data/chroma_db/
.ingested.json
prompts/
```

- [ ] **Step 4: Install dependencies**

Run: `uv sync`

Expected: dependencies downloaded, `.venv` updated

- [ ] **Step 5: Create data directories**

Run: `mkdir -p data/documents`

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml .gitignore data/documents/.gitkeep
touch data/documents/.gitkeep
git add data/documents/.gitkeep
git commit -m "feat: add pyproject.toml and project structure for RAG module"
```

---

### Task 2: Configuration Module

**Files:**
- Create: `config.py`

将 `nanocode.py` 中的配置提取到独立模块，新增 Embedding 和 RAG 路径配置。

- [ ] **Step 1: Create config.py**

```python
"""Configuration management - API, Embedding, and path settings."""

import os

import dotenv

dotenv.load_dotenv()

# --- LLM API ---
API_URL = os.environ.get("ALI_API_URL", "")
API_KEY = os.environ.get("ALI_API_KEY", "")
MODEL = os.environ.get("ALI_MODEL", "qwen3.5-flash")

# --- Embedding API ---
EMBEDDING_API_URL = os.environ.get(
    "EMBEDDING_API_URL", "https://dashscope.aliyuncs.com/compatible-mode/v1"
)
EMBEDDING_API_KEY = os.environ.get("EMBEDDING_API_KEY", API_KEY)
EMBEDDING_MODEL = os.environ.get("EMBEDDING_MODEL", "text-embedding-v3")

# --- RAG Paths ---
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DOCUMENTS_DIR = os.environ.get(
    "DOCUMENTS_DIR", os.path.join(BASE_DIR, "data", "documents")
)
CHROMA_PERSIST_DIR = os.environ.get(
    "CHROMA_PERSIST_DIR", os.path.join(BASE_DIR, "data", "chroma_db")
)

# --- ANSI colors ---
RESET, BOLD, DIM = "\033[0m", "\033[1m", "\033[2m"
BLUE, CYAN, GREEN, YELLOW, RED = (
    "\033[34m",
    "\033[36m",
    "\033[32m",
    "\033[33m",
    "\033[31m",
)
```

- [ ] **Step 2: Verify config loads correctly**

Run: `uv run python -c "import config; print(f'MODEL={config.MODEL}, EMBEDDING_MODEL={config.EMBEDDING_MODEL}, DOC_DIR={config.DOCUMENTS_DIR}')"`

Expected: prints config values without error

- [ ] **Step 3: Commit**

```bash
git add config.py
git commit -m "feat: add config module with API, Embedding, and RAG path settings"
```

---

### Task 3: RAG Document Loader

**Files:**
- Create: `rag/__init__.py` (empty)
- Create: `rag/loader.py`

- [ ] **Step 1: Create rag package**

Create `rag/__init__.py` as empty file (will be populated in Task 7).

- [ ] **Step 2: Create rag/loader.py**

```python
"""Document loading via LlamaIndex SimpleDirectoryReader."""

from pathlib import Path

from llama_index.core import SimpleDirectoryReader


def load_documents(doc_dir: str) -> list:
    """Load all supported documents from a directory.

    Returns list of LlamaIndex Document objects with metadata
    (file_path, file_name, file_type, file_size).
    """
    path = Path(doc_dir)
    if not path.exists():
        raise FileNotFoundError(f"Document directory not found: {doc_dir}")

    supported_ext = [".pdf", ".md", ".txt", ".markdown"]
    reader = SimpleDirectoryReader(
        input_dir=str(path),
        required_exts=supported_ext,
        recursive=True,
    )
    documents = reader.load_data()
    return documents


def load_single_file(file_path: str) -> list:
    """Load a single file into LlamaIndex Document objects."""
    p = Path(file_path)
    if not p.exists():
        raise FileNotFoundError(f"File not found: {file_path}")

    reader = SimpleDirectoryReader(input_files=[str(p)])
    return reader.load_data()
```

- [ ] **Step 3: Create a test document and verify loading**

Run:
```bash
mkdir -p data/documents
cat > data/documents/test_sample.md << 'EOF'
# 苹果公司2025年第一季度财报摘要

苹果公司2025年第一季度营收为1196亿美元，同比增长14%。
iPhone收入达到697亿美元，同比增长12%。
服务收入达到231亿美元，创历史新高。

Mac收入为89亿美元，同比下降3%。
iPad收入为82亿美元，同比增长15%。

公司管理层对2025年第二季度持乐观态度，预计营收将在1200-1240亿美元之间。
EOF

uv run python -c "
from rag.loader import load_documents
docs = load_documents('data/documents')
print(f'Loaded {len(docs)} documents')
for d in docs:
    print(f'  - {d.metadata.get(\"file_name\", \"unknown\")}: {len(d.text)} chars')
"
```

Expected: `Loaded 1 documents` with file name and character count

- [ ] **Step 4: Commit**

```bash
git add rag/__init__.py rag/loader.py
git commit -m "feat: add RAG document loader (PDF/MD/TXT via SimpleDirectoryReader)"
```

---

### Task 4: RAG Text Chunker

**Files:**
- Create: `rag/chunker.py`

- [ ] **Step 1: Create rag/chunker.py**

```python
"""Text chunking via LlamaIndex SentenceSplitter."""

from llama_index.core.node_parser import SentenceSplitter


def create_splitter(chunk_size: int = 1024, chunk_overlap: int = 100):
    """Create a SentenceSplitter instance.

    chunk_size=1024 and chunk_overlap=100 are tuned for Chinese financial
    reports where characters are information-dense.
    """
    return SentenceSplitter(
        chunk_size=chunk_size,
        chunk_overlap=chunk_overlap,
    )


def split_documents(documents: list, chunk_size: int = 1024, chunk_overlap: int = 100) -> list:
    """Split documents into nodes (chunks).

    Returns list of LlamaIndex TextNode objects, each with
    text content and inherited metadata.
    """
    splitter = create_splitter(chunk_size, chunk_overlap)
    nodes = splitter.get_nodes_from_documents(documents)
    return nodes
```

- [ ] **Step 2: Verify chunking works**

Run:
```bash
uv run python -c "
from rag.loader import load_documents
from rag.chunker import split_documents

docs = load_documents('data/documents')
nodes = split_documents(docs)
print(f'Split {len(docs)} docs into {len(nodes)} chunks')
for i, node in enumerate(nodes):
    print(f'  chunk {i}: {len(node.text)} chars, metadata={node.metadata}')
"
```

Expected: Documents split into chunks, each with metadata inherited from source document

- [ ] **Step 3: Commit**

```bash
git add rag/chunker.py
git commit -m "feat: add RAG text chunker (SentenceSplitter for Chinese financial docs)"
```

---

### Task 5: RAG Index Builder

**Files:**
- Create: `rag/indexer.py`

这是最复杂的 RAG 组件。负责管理 Chroma 向量数据库、构建索引、增量更新。

- [ ] **Step 1: Create rag/indexer.py**

```python
"""Chroma vector index management with LlamaIndex."""

import hashlib
import json
import os
from pathlib import Path

import chromadb
from llama_index.core import StorageContext, VectorStoreIndex
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.vector_stores.chroma import ChromaVectorStore

from rag.chunker import split_documents


def _get_embed_model(api_url: str, api_key: str, model_name: str):
    """Create OpenAI-compatible embedding model."""
    return OpenAIEmbedding(
        model=model_name,
        api_key=api_key,
        api_base=api_url,
    )


def _ingested_tracker_path(chroma_dir: str) -> str:
    """Path to the JSON file tracking which files have been ingested."""
    return os.path.join(os.path.dirname(chroma_dir), ".ingested.json")


def _load_ingested(tracker_path: str) -> dict:
    """Load the ingested files tracker. Returns {filename: md5hash}."""
    if not os.path.exists(tracker_path):
        return {}
    with open(tracker_path, "r") as f:
        return json.load(f)


def _save_ingested(tracker_path: str, data: dict):
    """Save the ingested files tracker."""
    os.makedirs(os.path.dirname(tracker_path), exist_ok=True)
    with open(tracker_path, "w") as f:
        json.dump(data, f, indent=2, ensure_ascii=False)


def _file_md5(file_path: str) -> str:
    """Compute MD5 hash of a file."""
    h = hashlib.md5()
    with open(file_path, "rb") as f:
        for chunk in iter(lambda: f.read(8192), b""):
            h.update(chunk)
    return h.hexdigest()


def get_index(
    chroma_dir: str,
    embedding_api_url: str,
    embedding_api_key: str,
    embedding_model_name: str,
):
    """Get or create a VectorStoreIndex backed by Chroma.

    Returns (index, embed_model) tuple.
    """
    embed_model = _get_embed_model(embedding_api_url, embedding_api_key, embedding_model_name)

    os.makedirs(chroma_dir, exist_ok=True)
    chroma_client = chromadb.PersistentClient(path=chroma_dir)
    collection = chroma_client.get_or_create_collection("financial_docs")
    vector_store = ChromaVectorStore(chroma_collection=collection)

    index = VectorStoreIndex.from_vector_store(vector_store, embed_model=embed_model)
    return index, embed_model


def add_to_index(
    index,
    documents: list,
    chroma_dir: str,
    doc_dir: str,
    chunk_size: int = 1024,
    chunk_overlap: int = 100,
) -> int:
    """Add new/modified documents to the index. Returns count of docs added.

    Uses an ingested tracker (.ingested.json) to skip files already processed
    with the same content hash.
    """
    tracker_path = _ingested_tracker_path(chroma_dir)
    ingested = _load_ingested(tracker_path)

    # Filter to only new or modified documents
    new_docs = []
    for doc in documents:
        file_path = doc.metadata.get("file_path", "")
        file_name = doc.metadata.get("file_name", "unknown")
        if not file_path or not os.path.exists(file_path):
            continue
        current_hash = _file_md5(file_path)
        if ingested.get(file_path) == current_hash:
            continue
        new_docs.append(doc)
        ingested[file_path] = current_hash

    if not new_docs:
        return 0

    # Split and insert
    nodes = split_documents(new_docs, chunk_size, chunk_overlap)
    index.insert_nodes(nodes)

    # Update tracker
    _save_ingested(tracker_path, ingested)
    return len(new_docs)


def build_fresh_index(
    documents: list,
    chroma_dir: str,
    embedding_api_url: str,
    embedding_api_key: str,
    embedding_model_name: str,
    chunk_size: int = 1024,
    chunk_overlap: int = 100,
):
    """Build a fresh index from documents. Used when no index exists yet.

    Returns (index, embed_model, doc_count) tuple.
    """
    embed_model = _get_embed_model(embedding_api_url, embedding_api_key, embedding_model_name)

    os.makedirs(chroma_dir, exist_ok=True)
    chroma_client = chromadb.PersistentClient(path=chroma_dir)
    collection = chroma_client.get_or_create_collection("financial_docs")
    vector_store = ChromaVectorStore(chroma_collection=collection)

    storage_context = StorageContext.from_defaults(vector_store=vector_store)
    nodes = split_documents(documents, chunk_size, chunk_overlap)

    index = VectorStoreIndex(
        nodes=nodes,
        storage_context=storage_context,
        embed_model=embed_model,
    )

    # Save tracker
    tracker_path = _ingested_tracker_path(chroma_dir)
    ingested = {}
    for doc in documents:
        file_path = doc.metadata.get("file_path", "")
        if file_path and os.path.exists(file_path):
            ingested[file_path] = _file_md5(file_path)
    _save_ingested(tracker_path, ingested)

    return index, embed_model, len(documents)
```

- [ ] **Step 2: Verify index building**

Run:
```bash
uv run python -c "
import config
from rag.loader import load_documents
from rag.indexer import build_fresh_index

docs = load_documents(config.DOCUMENTS_DIR)
print(f'Loaded {len(docs)} documents')

index, embed_model, count = build_fresh_index(
    docs,
    config.CHROMA_PERSIST_DIR,
    config.EMBEDDING_API_URL,
    config.EMBEDDING_API_KEY,
    config.EMBEDDING_MODEL,
)
print(f'Built index with {count} documents')
"
```

Expected: Index built without error, `data/chroma_db/` directory created with Chroma data

- [ ] **Step 3: Commit**

```bash
git add rag/indexer.py
git commit -m "feat: add RAG index builder with Chroma + incremental ingestion"
```

---

### Task 6: RAG Retriever

**Files:**
- Create: `rag/retriever.py`

- [ ] **Step 1: Create rag/retriever.py**

```python
"""Similarity-based retrieval from Chroma vector index."""


def retrieve(index, question: str, top_k: int = 5) -> list[dict]:
    """Search the index for relevant document chunks.

    Returns list of dicts:
        [{"text": "...", "source": "filename", "score": 0.85}, ...]
    sorted by relevance (highest first).
    """
    retriever = index.as_retriever(similarity_top_k=top_k)
    nodes_with_scores = retriever.retrieve(question)

    results = []
    for nws in nodes_with_scores:
        results.append({
            "text": nws.node.text,
            "source": nws.node.metadata.get("file_name", "unknown"),
            "score": round(nws.score, 4) if nws.score is not None else 0.0,
        })
    return results


def format_results(results: list[dict]) -> str:
    """Format retrieval results into a readable string for LLM consumption."""
    if not results:
        return "未在本地知识库中找到相关信息。"

    parts = [f"从本地知识库检索到 {len(results)} 条相关信息：\n"]
    for i, r in enumerate(results, 1):
        parts.append(f"[来源: {r['source']}，相关度: {r['score']:.2f}]")
        parts.append(f"{r['text']}\n")
    return "\n".join(parts)
```

- [ ] **Step 2: Verify retrieval works (requires index from Task 5)**

Run:
```bash
uv run python -c "
import config
from rag.indexer import get_index
from rag.retriever import retrieve, format_results

index, embed_model = get_index(
    config.CHROMA_PERSIST_DIR,
    config.EMBEDDING_API_URL,
    config.EMBEDDING_API_KEY,
    config.EMBEDDING_MODEL,
)

results = retrieve(index, '苹果公司最近财报表现如何', top_k=3)
print(format_results(results))
"
```

Expected: Returns relevant chunks from the test document with source names and scores

- [ ] **Step 3: Commit**

```bash
git add rag/retriever.py
git commit -m "feat: add RAG retriever with top-k similarity search and result formatting"
```

---

### Task 7: RAG Package Interface

**Files:**
- Modify: `rag/__init__.py`

将 `rag/` 包暴露为两个简单函数：`ingest()` 和 `query()`。

- [ ] **Step 1: Write rag/__init__.py**

```python
"""RAG module - document ingestion and semantic retrieval.

Usage:
    import rag

    # Import documents into knowledge base
    count = rag.ingest("/path/to/docs")

    # Query the knowledge base
    results = rag.query("苹果公司财报表现如何")
"""

import os

import config
from rag.indexer import add_to_index, build_fresh_index, get_index
from rag.loader import load_documents
from rag.retriever import format_results, retrieve


def ingest(doc_path: str | None = None) -> int:
    """Import documents into the financial knowledge base.

    Args:
        doc_path: Directory containing documents. Defaults to config.DOCUMENTS_DIR.

    Returns:
        Number of new documents processed.
    """
    doc_dir = doc_path or config.DOCUMENTS_DIR
    documents = load_documents(doc_dir)
    if not documents:
        return 0

    chroma_dir = config.CHROMA_PERSIST_DIR

    # Check if index already exists
    tracker_file = os.path.join(os.path.dirname(chroma_dir), ".ingested.json")
    if os.path.exists(tracker_file) or os.path.exists(chroma_dir):
        # Incremental update
        index, _ = get_index(
            chroma_dir,
            config.EMBEDDING_API_URL,
            config.EMBEDDING_API_KEY,
            config.EMBEDDING_MODEL,
        )
        return add_to_index(index, documents, chroma_dir, doc_dir)
    else:
        # Fresh build
        _, _, count = build_fresh_index(
            documents,
            chroma_dir,
            config.EMBEDDING_API_URL,
            config.EMBEDDING_API_KEY,
            config.EMBEDDING_MODEL,
        )
        return count


def query(question: str, top_k: int = 5) -> list[dict]:
    """Search the knowledge base for relevant information.

    Args:
        question: Natural language question.
        top_k: Number of results to return.

    Returns:
        List of {"text": str, "source": str, "score": float}.
    """
    index, _ = get_index(
        config.CHROMA_PERSIST_DIR,
        config.EMBEDDING_API_URL,
        config.EMBEDDING_API_KEY,
        config.EMBEDDING_MODEL,
    )
    return retrieve(index, question, top_k)


def query_formatted(question: str, top_k: int = 5) -> str:
    """Search and return formatted results string (for tool output)."""
    results = query(question, top_k)
    return format_results(results)
```

- [ ] **Step 2: Verify the public interface**

Run:
```bash
uv run python -c "
import rag

# Test ingest
count = rag.ingest()
print(f'Ingested {count} new documents')

# Test query
results = rag.query('苹果公司营收')
print(f'Found {len(results)} results')
for r in results:
    print(f'  [{r[\"source\"]}] score={r[\"score\"]}')

# Test formatted query
print(rag.query_formatted('苹果公司营收'))
"
```

Expected: ingest returns 0 (already ingested), query returns results

- [ ] **Step 3: Commit**

```bash
git add rag/__init__.py
git commit -m "feat: add RAG package with ingest() and query() public interface"
```

---

### Task 8: Tools Module

**Files:**
- Create: `tools.py`

从 `nanocode.py` 提取 6 个现有工具 + 新增 2 个 RAG 工具。

- [ ] **Step 1: Create tools.py**

```python
"""Tool implementations and registry for the agentic loop."""

import glob as globlib
import json
import os
import re
import subprocess


# --- Original tool implementations ---


def read(args):
    lines = open(args["path"]).readlines()
    offset = args.get("offset", 0)
    limit = args.get("limit", len(lines))
    selected = lines[offset : offset + limit]
    return "".join(f"{offset + idx + 1:4}| {line}" for idx, line in enumerate(selected))


def write(args):
    with open(args["path"], "w") as f:
        f.write(args["content"])
    return "ok"


def edit(args):
    text = open(args["path"]).read()
    old, new = args["old"], args["new"]
    if old not in text:
        return "error: old_string not found"
    count = text.count(old)
    if not args.get("all") and count > 1:
        return f"error: old_string appears {count} times, must be unique (use all=true)"
    replacement = text.replace(old, new) if args.get("all") else text.replace(old, new, 1)
    with open(args["path"], "w") as f:
        f.write(replacement)
    return "ok"


def glob(args):
    pattern = (args.get("path", ".") + "/" + args["pat"]).replace("//", "/")
    files = globlib.glob(pattern, recursive=True)
    files = sorted(
        files,
        key=lambda f: os.path.getmtime(f) if os.path.isfile(f) else 0,
        reverse=True,
    )
    return "\n".join(files) or "none"


def grep(args):
    pattern = re.compile(args["pat"])
    hits = []
    for filepath in globlib.glob(args.get("path", ".") + "/**", recursive=True):
        try:
            for line_num, line in enumerate(open(filepath), 1):
                if pattern.search(line):
                    hits.append(f"{filepath}:{line_num}:{line.rstrip()}")
        except Exception:
            pass
    return "\n".join(hits[:50]) or "none"


def bash(args):
    proc = subprocess.Popen(
        args["cmd"],
        shell=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        text=True,
    )
    output_lines = []
    try:
        while True:
            line = proc.stdout.readline()
            if not line and proc.poll() is not None:
                break
            if line:
                print(f"  \033[2m│ {line.rstrip()}\033[0m", flush=True)
                output_lines.append(line)
        proc.wait(timeout=30)
    except subprocess.TimeoutExpired:
        proc.kill()
        output_lines.append("\n(timed out after 30s)")
    return "".join(output_lines).strip() or "(empty)"


# --- RAG tool implementations ---


def rag_query_tool(args):
    """Tool wrapper: search local knowledge base."""
    import rag as rag_module

    question = args["question"]
    top_k = args.get("top_k", 5)
    return rag_module.query_formatted(question, top_k)


def rag_ingest_tool(args):
    """Tool wrapper: import documents into knowledge base."""
    import rag as rag_module

    doc_path = args.get("path") or None
    count = rag_module.ingest(doc_path)
    if count == 0:
        return "没有新文档需要导入（所有文档已在知识库中）。"
    return f"成功导入 {count} 个新文档到知识库。"


# --- Tool registry ---
# (description, param_schema, function)

TOOLS = {
    "read": (
        "Read file with line numbers (file path, not directory)",
        {"path": "string", "offset": "number?", "limit": "number?"},
        read,
    ),
    "write": (
        "Write content to file",
        {"path": "string", "content": "string"},
        write,
    ),
    "edit": (
        "Replace old with new in file (old must be unique unless all=true)",
        {"path": "string", "old": "string", "new": "string", "all": "boolean?"},
        edit,
    ),
    "glob": (
        "Find files by pattern, sorted by mtime",
        {"pat": "string", "path": "string?"},
        glob,
    ),
    "grep": (
        "Search files for regex pattern",
        {"pat": "string", "path": "string?"},
        grep,
    ),
    "bash": (
        "Run shell command",
        {"cmd": "string"},
        bash,
    ),
    "rag_query": (
        "搜索本地金融知识库（研报、新闻、分析文档）。当用户询问金融、股票、市场相关问题时使用此工具获取相关信息。",
        {"question": "string", "top_k": "number?"},
        rag_query_tool,
    ),
    "rag_ingest": (
        "将文档导入本地金融知识库。支持 PDF 和 Markdown 文件。当用户要求导入或更新文档时使用。",
        {"path": "string"},
        rag_ingest_tool,
    ),
}


def run_tool(name, args):
    """Execute a tool by name with given arguments."""
    try:
        return TOOLS[name][2](args)
    except Exception as err:
        return f"error: {err}"


def make_schema():
    """Convert TOOLS registry into OpenAI function calling format."""
    result = []
    for name, (description, params, _fn) in TOOLS.items():
        properties = {}
        required = []
        for param_name, param_type in params.items():
            is_optional = param_type.endswith("?")
            base_type = param_type.rstrip("?")
            properties[param_name] = {
                "type": "integer" if base_type == "number" else base_type
            }
            if not is_optional:
                required.append(param_name)
        result.append(
            {
                "type": "function",
                "function": {
                    "name": name,
                    "description": description,
                    "parameters": {
                        "type": "object",
                        "properties": properties,
                        "required": required,
                    },
                },
            }
        )
    return result
```

- [ ] **Step 2: Verify tools module loads**

Run:
```bash
uv run python -c "
from tools import TOOLS, make_schema
print(f'Registered {len(TOOLS)} tools:')
for name in TOOLS:
    print(f'  - {name}')
schema = make_schema()
print(f'Schema has {len(schema)} tool definitions')
rag_names = [t['function']['name'] for t in schema if 'rag' in t['function']['name']]
print(f'RAG tools in schema: {rag_names}')
"
```

Expected: 8 tools registered, including `rag_query` and `rag_ingest`

- [ ] **Step 3: Commit**

```bash
git add tools.py
git commit -m "feat: extract tools module with 8 tools including rag_query and rag_ingest"
```

---

### Task 9: Refactor nanocode.py

**Files:**
- Modify: `nanocode.py`

将 `nanocode.py` 重构为从 `config` 和 `tools` 模块导入，保留核心的 `call_api()` 和 `main()` loop。

- [ ] **Step 1: Rewrite nanocode.py**

```python
#!/usr/bin/env python3
"""nanocode-financial - 个人金融智能分析助手"""

import json
import os
import re
import urllib.request

import config
from tools import make_schema, run_tool


def call_api(messages, system_prompt):
    """调用 LLM API (OpenAI Chat Completions compatible)."""
    all_messages = [{"role": "system", "content": system_prompt}] + messages
    request = urllib.request.Request(
        config.API_URL,
        data=json.dumps(
            {
                "model": config.MODEL,
                "max_tokens": 8192,
                "messages": all_messages,
                "tools": make_schema(),
            }
        ).encode(),
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {config.API_KEY}",
        },
    )
    response = urllib.request.urlopen(request)
    return json.loads(response.read())


def separator():
    """终端分隔线"""
    return f"{config.DIM}{'─' * min(os.get_terminal_size().columns, 80)}{config.RESET}"


def render_markdown(text):
    """渲染 Markdown（仅支持 **加粗**）"""
    return re.sub(r"\*\*(.+?)\*\*", f"{config.BOLD}\\1{config.RESET}", text)


def load_system_prompt() -> str:
    """加载系统提示词，优先从 prompts/rag_system.txt，否则用默认。"""
    prompt_path = os.path.join(config.BASE_DIR, "prompts", "rag_system.txt")
    if os.path.exists(prompt_path):
        with open(prompt_path, "r") as f:
            return f.read()
    return f"你是一个金融分析助手。当前工作目录: {os.getcwd()}"


def main():
    """主循环：读取用户输入，驱动 agentic loop 与模型交互。"""
    print(
        f"{config.BOLD}nanocode-financial{RESET} | "
        f"{config.DIM}{config.MODEL} | {os.getcwd()}{config.RESET}\n"
    )
    messages = []
    system_prompt = load_system_prompt()

    while True:
        try:
            print(separator())
            user_input = input(f"{config.BOLD}{config.BLUE}❯{config.RESET} ").strip()
            print(separator())

            if not user_input:
                continue
            if user_input in ("/q", "exit"):
                break
            if user_input == "/clear":
                messages = []
                print(f"{config.GREEN}⏺ Cleared conversation{config.RESET}")
                continue

            messages.append({"role": "user", "content": user_input})

            # Agentic loop
            while True:
                response = call_api(messages, system_prompt)
                msg = response["choices"][0]["message"]
                tool_calls = msg.get("tool_calls") or []
                tool_results = []

                if msg.get("content"):
                    print(f"\n{config.CYAN}⏺{config.RESET} {render_markdown(msg['content'])}")

                for tc in tool_calls:
                    tool_name = tc["function"]["name"]
                    tool_args = json.loads(tc["function"]["arguments"])
                    arg_preview = str(list(tool_args.values())[0])[:50]
                    print(
                        f"\n{config.GREEN}⏺ {tool_name.capitalize()}{config.RESET}"
                        f"({config.DIM}{arg_preview}{config.RESET})"
                    )

                    result = run_tool(tool_name, tool_args)
                    result_lines = result.split("\n")
                    preview = result_lines[0][:60]
                    if len(result_lines) > 1:
                        preview += f" ... +{len(result_lines) - 1} lines"
                    elif len(result_lines[0]) > 60:
                        preview += "..."
                    print(f"  {config.DIM}⎿  {preview}{config.RESET}")

                    tool_results.append(
                        {
                            "role": "tool",
                            "tool_call_id": tc["id"],
                            "content": result,
                        }
                    )

                messages.append(msg)

                if not tool_results:
                    break
                messages.extend(tool_results)

            print()

        except (KeyboardInterrupt, EOFError):
            break
        except Exception as err:
            print(f"{config.RED}⏺ Error: {err}{config.RESET}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Verify the refactored app starts**

Run: `uv run python nanocode.py`

Expected: Prints welcome banner with model name, shows `❯` prompt. Type `exit` to quit.

Verify existing tools still work: at the `❯` prompt, type `读取当前目录的文件列表` and confirm the LLM uses the `bash` tool.

- [ ] **Step 3: Commit**

```bash
git add nanocode.py
git commit -m "refactor: nanocode.py now imports from config and tools modules"
```

---

### Task 10: System Prompt

**Files:**
- Create: `prompts/rag_system.txt`

- [ ] **Step 1: Create prompts directory and system prompt**

```
mkdir -p prompts
```

Create `prompts/rag_system.txt`:

```
你是一个专业的金融分析助手，具备以下能力：

1. 金融知识问答：当用户询问金融、股票、市场、公司基本面等相关问题时，使用 rag_query 工具从本地知识库中检索相关信息，基于检索到的资料给出回答，并附上引用来源。

2. 文档管理：当用户要求导入文档、更新知识库时，使用 rag_ingest 工具。

3. 代码与文件操作：你同时也具备文件读写和命令执行能力，可以帮助用户处理代码相关任务。

回答原则：
- 优先使用 rag_query 检索本地知识库中的信息来回答金融问题
- 如果知识库中没有相关信息，可以明确告知用户
- 回答金融问题时，附上信息来源（文档名）
- 回答要简洁、结构化、重点突出

当前工作目录: {cwd}
```

注意：`{cwd}` 将在 `load_system_prompt()` 中被替换。更新 `nanocode.py` 的 `load_system_prompt()`：

- [ ] **Step 2: Update load_system_prompt in nanocode.py to handle {cwd} placeholder**

Replace the `load_system_prompt` function in `nanocode.py`:

```python
def load_system_prompt() -> str:
    """加载系统提示词，优先从 prompts/rag_system.txt，否则用默认。"""
    prompt_path = os.path.join(config.BASE_DIR, "prompts", "rag_system.txt")
    if os.path.exists(prompt_path):
        with open(prompt_path, "r") as f:
            return f.read().replace("{cwd}", os.getcwd())
    return f"你是一个金融分析助手。当前工作目录: {os.getcwd()}"
```

- [ ] **Step 3: Verify system prompt loads**

Run:
```bash
uv run python -c "
from nanocode import load_system_prompt
prompt = load_system_prompt()
print(prompt[:200])
print('...')
print(f'Total length: {len(prompt)} chars')
"
```

Expected: Prints the system prompt with `{cwd}` replaced by actual working directory

- [ ] **Step 4: Commit**

```bash
git add prompts/rag_system.txt nanocode.py
git commit -m "feat: add RAG system prompt for financial assistant persona"
```

---

### Task 11: End-to-End Validation

**Files:**
- Create: `data/documents/test_apple_report.md` (sample data)

用完整的端到端流程验证 RAG 功能。

- [ ] **Step 1: Create additional test documents**

Create `data/documents/test_market_analysis.md`:

```markdown
# 2025年科技板块市场分析

## 市场概况

2025年第一季度，全球科技板块表现强劲，纳斯达克指数上涨12.3%。AI概念股持续受到资金追捧，其中英伟达涨幅超过40%。

## 重点公司

### 苹果 (AAPL)
- 营收：1196亿美元（同比+14%）
- iPhone收入：697亿美元（同比+12%）
- 服务收入：231亿美元，创历史新高
- 估值：市盈率28倍，略高于行业平均

### 特斯拉 (TSLA)
- 交付量：48.3万辆，低于市场预期的50万辆
- 营收：251亿美元
- 毛利率：17.4%，持续承压
- 自动驾驶FSD V13获得积极反馈

### 英伟达 (NVDA)
- 数据中心收入：220亿美元（同比+409%）
- AI芯片需求持续旺盛
- 市值突破3万亿美元

## 风险提示

1. 美联储降息节奏不确定，可能影响科技股估值
2. AI投资回报期较长，部分公司可能出现盈利不及预期
3. 中美科技竞争加剧，半导体行业面临政策风险
```

- [ ] **Step 2: Ingest the new document**

Run:
```bash
uv run python -c "
import rag
count = rag.ingest()
print(f'Ingested {count} new documents')

# Verify total
results = rag.query('特斯拉毛利率', top_k=3)
for r in results:
    print(f'[{r[\"source\"]}] score={r[\"score\"]}')
    print(f'  {r[\"text\"][:100]}...')
"
```

Expected: 1 new document ingested, query about Tesla returns relevant chunk

- [ ] **Step 3: Test the full application interactively**

Run: `uv run python nanocode.py`

Test the following queries at the `❯` prompt:

1. `"苹果公司的营收是多少？"` → Should trigger `rag_query`, return answer with source citation
2. `"导入 data/documents 目录下的新文档"` → Should trigger `rag_ingest`
3. `"帮我看看当前目录有什么文件"` → Should trigger `bash` tool (existing functionality preserved)

Type `exit` to quit.

- [ ] **Step 4: Final commit**

```bash
git add data/documents/test_market_analysis.md
git commit -m "test: add sample market analysis document for RAG validation"
```

---

## Summary

| Task | What it builds | Key files |
|------|---------------|-----------|
| 1 | Project infrastructure | pyproject.toml, .env, .gitignore |
| 2 | Configuration module | config.py |
| 3 | Document loader | rag/loader.py |
| 4 | Text chunker | rag/chunker.py |
| 5 | Index builder | rag/indexer.py |
| 6 | Retriever | rag/retriever.py |
| 7 | Package interface | rag/__init__.py |
| 8 | Tools module | tools.py |
| 9 | Main app refactor | nanocode.py |
| 10 | System prompt | prompts/rag_system.txt |
| 11 | End-to-end validation | test documents + interactive test |
